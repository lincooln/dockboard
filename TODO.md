# TODO: Docker Dashboard - v2.0 Roadmap

**Функционал:**
- добавить возможность установить обои в качестве фона. при установке обоев тема подбирается на основе цветов обоев ток чтобы текст хорошо читался а элементы гармонировали с выбранными обоями, плитки выглядили как "стеклянные"
- при удалении обоев устанавливалась тема по умолчанию (тёмная)
- 
- Таблица со всеми контейнерами и их детальной статистикой в реальном времени.
- Колонки:
  - Имя контейнера (с иконкой)
  - Статус (цветная точка + текст)
  - Использование CPU (% и графика)
  - Использование памяти (абсолютное значение и % от лимита/хоста)
  - Использование дискового I/O (чтение/запись)
  - Использование сети I/O (входящий/исходящий трафик)
  - Имена сетей
  - Количество процессов (PIDs)
  - колонка действия (кнопка остановка для запущенного контейнера/кнопка запуск для остановленного контейнера)
- клик по любому заголовку колонок выполняет сортировку списка контейнеров по возрастанию, повторный клик по убыванию.
- Фильтрация по частичному совпадению имени сервиса, контейнера или стека.
- Автообновление данных (с настраиваемым интервалом: 5, 10, 30 сек).

**Технические задачи:**
- Изучить и реализовать сбор данных через `docker stats` API (`client.containers.get(container_id).stats(stream=False)`).
- Создать новый endpoint `/api/containers/stats` для JSON-данных.
- Разработать шаблон `templates/details.html`.
- Реализовать фронтенд для динамического обновления таблицы.
- Создать API endpoints (`/api/container/<id>/start`, `/api/container/<id>/stop`).
- Интегрировать вызовы API в фронтенд.
- Обеспечить безопасность (проверка ID, обработка ошибок Docker).

**Безопасность и надежность**
- **Обработка ошибок Docker API:** Устойчивость к отключению демона, отсутствию контейнера.
- **Валидация входных данных:** Во всех новых API endpoints.
- **Производительность:** Оптимизация частоты опроса `docker stats` для большого числа контейнеров.
- **Документация:** Обновить `README.md` с описанием нового функционала и требованиями.

---

## 📅 Предлагаемый порядок реализации

1.  Статус ✅ Реализовать сбор детальной статистики по контейнерам через Docker Stats API (бэкенд). *(Выполнено: функция `get_detailed_container_stats()` в `system_stats.py`, API endpoint `/api/containers/stats`)*
2. Статус ✅ Реализовать API для управления контейнерами (запуск/остановка). *(Выполнено: endpoints `/api/container/<id>/start`, `/api/container/<id>/stop`)*
3. Проверить наличие неиспользуемого кода (скрипты, методы, функции) оставшегося от прошлых версий.
4. Проверить css стили на наличие неиспользуемых стилей.

**Тестирование, рефакторинг, документация.** *(Частично: документация обновлена, тестирование не проведено, найденные баги не устранены)*
5. починить чекбокс инверсии темы. сейчас первый клик не инверсирует тему. второй клик инверсирует но при этом чекбокс снимается.
6. починить саму инверсию. сейчас при тёмной теме инверсия изменяет цвета на розовые вместо свеьлых близких к белому. правильная логика инверсные цвета должны быть антогонистами текущей темы (не точными, а близкими, так чтобы тёмная становилась светлой а светлая становилась тёмной но при этом цвета были подобраны гармонично).

