# TODO: Docker Dashboard 

- добавить возможность установить обои в качестве фона. при установке обоев тема подбирается на основе цветов обоев ток чтобы текст хорошо читался а элементы гармонировали с выбранными обоями, плитки выглядили как "стеклянные"
при удалении обоев устанавливалась тема по умолчанию (тёмная)
 
- Таблица со всеми контейнерами в реальном времени.
- Колонки:
  - Имя контейнера c ntujv (например для nginx это nginx:latest)
  - Статус (запущен/остановлен)
  - Использование CPU (абсолютное значение и % от лимита если лимит задан)
  - Использование памяти (абсолютное значение и % от лимита если лимит задан)
  - Использование дискового I/O (скорость чтение/запись)
  - Использование сети I/O (входящий/исходящий трафик)
  - Имена сетей с которыми связан контейнер (через запятую)
  - Количество процессов (PIDs)
  - колонка действия (кнопка остановка для запущенного контейнера/кнопка запуск для остановленного контейнера)
- клик по любому заголовку колонок выполняет сортировку списка контейнеров по возрастанию, повторный клик по убыванию.
- Фильтрация по частичному совпадению имени сервиса, контейнера или стека.
- сейчас данные обновляются с настраиваемым интервалом: 5, 10, 30 сек, как получить постоянное обновление?
- исключить изображение своего образа в списке запущенных контейнеров на главной панели при запуске приложения через docker контейнер.

устранить БАГ: сейчас на странице детализации если нажать на кнопку стоп то контейнер останавливается и запись на странице исчезает. т.е. я не вижу остановленные контейнеры. это не правильно. логика остановки тут заключается в том чтобы временно останавливать контейнеры (для освобождения процесссора и памяти) и снова запускать контейнеры по необходимости.

